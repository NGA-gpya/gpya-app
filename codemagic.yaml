workflows:
  ios-workflow:
    name: GPYA iOS Release
    max_build_duration: 120
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
      groups:
        - appstore_credentials
        - supabase_config
      vars:
        BUNDLE_ID: "com.grupopadillayaguilar.gpya"
    scripts:
      - name: Get Flutter packages
        script: flutter pub get

      - name: Set up iOS code signing
        script: |
          keychain initialize
          # Usamos el método de sesión si ya tienes el Developer Portal conectado
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --create
          keychain add-certificates
          xcode-project use-profiles

      - name: Build IPA
        script: |
          flutter build ipa --release \
            --build-name=1.0.0 \
            --build-number=$BUILD_NUMBER \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
    artifacts:
      - build/ios/ipa/*.ipa
    # Hemos eliminado la sección de Publishing e Integrations para evitar errores de validación
    # Ahora el build generará el archivo .ipa que podrás descargar directamente.
